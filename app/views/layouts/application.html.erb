<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Money Tracker</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <div class="container">
      <header>
        <h1>Money Tracker</h1>
      </header>

      <% if notice %>
        <p class="notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert"><%= alert %></p>
      <% end %>

      <nav>
        <ul>
          <% if account_signed_in? %>
            <li><%= image_tag current_account.gravatar_url %></li>
            <li><%= link_to "Home", root_path %></li>
            <li><%= link_to "Dashboard", dashboard_show_path %></li>
            <li><%= link_to "Expenses", expenses_path %></li>
            <li><%= link_to('My Account', edit_account_registration_path) %></li>
            <li><%= link_to "Sign Out", destroy_account_session_path, method: :delete %> </li>
          <% else %>
            <li><%= link_to "Sign in", new_account_session_path %> </li>
          <% end %>
        </ul>
      </nav>

      <%= yield %>

      <footer>
        <small>Copyright <%=Date.today.year %></small>
      </footer>
    </div>
  </body>
</html>
